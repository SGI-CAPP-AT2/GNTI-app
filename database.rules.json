{
  "rules": {
    "profiles": {
      "$user_id": {
        ".read": "$user_id === auth.uid",
        ".write": "$user_id === auth.uid"
      }
    },
    "sessions": {
      "$sessid": {
        ".read": "$user_id === auth.uid"
      }
    },
    ".read": false,
    ".write": false
  }
}
